import React from "react";
import AuthForm from "./AuthForm";

const UserContent = ({  }) => {
    return (
        <div>
            <AuthForm />
        </div>
    );
};

export default UserContent;